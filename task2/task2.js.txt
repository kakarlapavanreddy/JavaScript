JavaScript Day 2 Assignment
Topics Covered: Printing Statements, Console Methods, Data Types, Operators, Arrays, Objects
Instructions: No loops. No conditional statements. Only use topics covered in class.
Submission Deadline: Tomorrow – End of Day (EOD)


1. Welcome Program- Ask user name using prompt().
- Show alert: Hi , Welcome to JavaScript Training.
- Print the same message in console.

A) let userName = prompt("Enter your name:");
   alert("Hi"+userName+",Welcome to JavaScript Training.");
   console.log("Hi"+userName+",Welcome to JavaScript Training.");

2. Console Methods Practice
- Print 500 using console.log(), console.warn(), console.error().
- Use console.clear().

A) console.log(500);
   console.warn(500);
   console.error(500);
   console.clear();

3. Data Type Identification
- Create string, number, boolean, undefined, and null variables.
- Print value and type using typeof.

A) let language = "Java";
   let marks = 85;
   let isPassed = false;
   let notAssigned;
   let emptyValue = null;

   console.log(language, typeof language);
   console.log(marks, typeof marks);
   console.log(isPassed, typeof isPassed);
   console.log(notAssigned, typeof notAssigned);
   console.log(emptyValue, typeof emptyValue);


4. Arithmetic Operations
- Use let a = 20 and let b = 5.
- Perform +, -, *, /, %, ** and print results.

A) let a = 20;
   let b = 5;

   console.log("Add:", a + b);
   console.log("Subtract:", a - b);
   console.log("Multiply:", a * b);
   console.log("Divide:", a / b);
   console.log("Remainder:", a % b);
   console.log("Power:", a ** b);

5. Increment & Decrement
- Demonstrate pre-increment, post-increment, pre-decrement, post-decrement.
- Print variables clearly.

A) let value = 8;

   console.log("Pre Increment:", ++value);
   console.log("Post Increment:", value++);
   console.log("Current Value:", value);
 
   console.log("Pre Decrement:", --value);
   console.log("Post Decrement:", value--);
   console.log("Final Value:", value); 

6. Assignment Operators
- Use let num = 10.
- Perform +=, -=, *=, /=, %= and print results.

A) let total = 10;

   total += 10;
   console.log("After += :", total);
 
   total -= 5;
   console.log("After -= :", total);

   total *= 2;
   console.log("After *= :", total);

   total /= 5;
   console.log("After /= :", total);
 
   total %= 4;
   console.log("After %= :", total);

7. Array Access- Create array ['HTML','CSS','JavaScript','React'].
- Print first element, second element, last element (using length), and total elements.

A) let technologies = ['HTML','CSS','JavaScript','React'];

   console.log("First Element:", technologies[0]);
   console.log("Second Element:", technologies[1]);
   console.log("Last Element:", technologies[technologies.length - 1]);
   console.log("Total Element:", technologies.length);

8. Modify Array
- Add one element at end.
- Remove last element.- Print updated array.

A) tools.push("Git");
   console.log("After Adding Tool:", tools);

   tools.pop();
   console.log("After Removing Tool:", tools);

9. Student Object
- Create object with name, age, course, city.
- Print values using dot notation.

A) let employee = {
   name: "Pavan",
   age: 24,
   course: "Software Developer",
   location: "Hyderabad"
  };

   console.log(employee.name);
   console.log(employee.age);
   console.log(employee.course);
   console.log(employee.location);

10. Nested Object Practice
- Create company object with nested trainer object.
- Print company name, trainer name, trainer subject.

A) let institute = {
   name: "Stackly",
   trainer: {
   name: "naveenkumar",
   subject: "JavaScript"
   }
 };

   console.log("company Name:", company.name);
   console.log("Trainer Name:", company.trainer.name);
   console.log("Trainer Subject:", company.trainer.subject);
